/**
 * Page-settings route — /campaigns/[campaignId]/pages/[pageId]
 * (?edit=general|seo|custom-code opens the corresponding settings panel)
 *
 * Renders nothing — see the base campaigns/[campaignId]/page.tsx comment.
 */
export default function PageSettingsRoute() {
  return null;
}
